<resources>

    <!-- Base application theme. -->
    <style name="Base.V24.Theme.HackApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="buttonStyle">@style/Widget.HackApp.Button</item>
        <item name="android:windowBackground">@color/dark_grey</item>
        <item name="android:statusBarColor">@color/dark_grey</item>
        <item name="colorPrimary">@color/orange</item>
    </style>

    <style name="Base.Theme.HackApp" parent="Base.V24.Theme.HackApp" />

    <style name="Theme.HackApp" parent="Base.Theme.HackApp" />

</resources>